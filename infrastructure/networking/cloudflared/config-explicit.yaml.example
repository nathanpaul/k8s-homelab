# OPTIONAL: Explicit Cloudflare Tunnel Config
# This is a more explicit version that lists each external service individually
# instead of using a wildcard. Use this if you want to be extra cautious.
#
# To use this:
# 1. Rename this file to config.yaml
# 2. Update kustomization.yaml to use this file instead
#
# NOTE: The wildcard approach in the default config.yaml is equally secure
# because DNS records control what reaches the tunnel. This is just more explicit.

tunnel: threadripper
protocol: http2
credentials-file: /etc/cloudflared/credentials/credentials.json
metrics: 0.0.0.0:2000
no-autoupdate: true
loglevel: info # Reduced from debug for production
transport-loglevel: info

ingress:
  # Test endpoint
  - hostname: hello.paulnathan.io
    service: hello_world

  # External services only (no internal services)
  - hostname: search.paulnathan.io
    service: https://cilium-gateway-gateway-external.gateway.svc.cluster.local:443
    originRequest:
      noTLSVerify: true
      originServerName: search.paulnathan.io

  - hostname: proxitok.paulnathan.io
    service: https://cilium-gateway-gateway-external.gateway.svc.cluster.local:443
    originRequest:
      noTLSVerify: true
      originServerName: proxitok.paulnathan.io

  - hostname: libreddit.paulnathan.io
    service: https://cilium-gateway-gateway-external.gateway.svc.cluster.local:443
    originRequest:
      noTLSVerify: true
      originServerName: libreddit.paulnathan.io

  - hostname: karakeep.paulnathan.io
    service: https://cilium-gateway-gateway-external.gateway.svc.cluster.local:443
    originRequest:
      noTLSVerify: true
      originServerName: karakeep.paulnathan.io

  - hostname: vert.paulnathan.io
    service: https://cilium-gateway-gateway-external.gateway.svc.cluster.local:443
    originRequest:
      noTLSVerify: true
      originServerName: vert.paulnathan.io

  - hostname: it-tools.paulnathan.io
    service: https://cilium-gateway-gateway-external.gateway.svc.cluster.local:443
    originRequest:
      noTLSVerify: true
      originServerName: it-tools.paulnathan.io

  # Catch-all 404
  - service: http_status:404
