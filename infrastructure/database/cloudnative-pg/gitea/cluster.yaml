apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: gitea-database
  namespace: cloudnative-pg
  labels:
    app: gitea
spec:
  instances: 1
  imageName: ghcr.io/cloudnative-pg/postgresql:16.2
  resources:
    requests:
      memory: 512Mi
      cpu: 250m
    limits:
      memory: 1Gi
  postgresql:
    parameters:
      shared_buffers: "128MB"
      max_wal_size: "512MB"
      wal_compression: "on"
    pg_hba:
      - host all all 0.0.0.0/0 md5
  # === NORMAL OPERATION: initdb (comment out for DR recovery) ===
  bootstrap:
    initdb:
      database: gitea
      owner: gitea
      secret:
        name: gitea-app-secret
  # === DISASTER RECOVERY ===
  # 1. Comment out the initdb bootstrap above
  # 2. Uncomment the recovery bootstrap + externalClusters below
  # 3. Update serverName in externalClusters to match CURRENT backup.serverName (gitea-database-v2)
  # 4. Bump backup.serverName to next version (e.g. gitea-database-v3)
  # 5. Apply directly with: kubectl kustomize ... | kubectl create -f -
  #    (ArgoCD SSA + CNPG webhook = initdb always wins, must bypass ArgoCD)
  # 6. After recovery completes, revert to initdb and push
  #
  # bootstrap:
  #   recovery:
  #     source: gitea-backup
  # externalClusters:
  #   - name: gitea-backup
  #     barmanObjectStore:
  #       serverName: gitea-database-v2
  #       destinationPath: s3://postgres-backups/cnpg/gitea
  #       endpointURL: http://192.168.10.133:30293
  #       s3Credentials:
  #         accessKeyId:
  #           name: cnpg-s3-credentials
  #           key: AWS_ACCESS_KEY_ID
  #         secretAccessKey:
  #           name: cnpg-s3-credentials
  #           key: AWS_SECRET_ACCESS_KEY
  #       wal:
  #         compression: gzip
  storage:
    size: 20Gi
    storageClass: longhorn
  walStorage:
    size: 10Gi
    storageClass: longhorn
  enableSuperuserAccess: true
  monitoring:
    enablePodMonitor: true
  backup:
    barmanObjectStore:
      serverName: gitea-database-v2
      destinationPath: s3://postgres-backups/cnpg/gitea
      endpointURL: http://192.168.10.133:30293
      s3Credentials:
        accessKeyId:
          name: cnpg-s3-credentials
          key: AWS_ACCESS_KEY_ID
        secretAccessKey:
          name: cnpg-s3-credentials
          key: AWS_SECRET_ACCESS_KEY
      wal:
        compression: gzip
      data:
        compression: gzip
    retentionPolicy: "14d"
