---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: kopia-ui-secrets
  namespace: kopia-ui
spec:
  refreshInterval: "1h"
  secretStoreRef:
    kind: ClusterSecretStore
    name: 1password
  target:
    name: kopia-ui-secret
    creationPolicy: Owner
  data:
    - secretKey: KOPIA_PASSWORD
      remoteRef:
        key: rustfs
        property: kopia_password
